{
  "name": "change-feed",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RSS for anything — turn any webpage into a change-feed using Firecrawl changeTracking.",
  "scripts": {
    "test": "vitest run",
    "watch": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mendable/firecrawl-js": "^4.16.0"
  },
  "devDependencies": {
    "tsx": "^4.22.3",
    "typescript": "^5.8.3",
    "vitest": "^2.1.8",
    "@types/node": "^22"
  }
}
